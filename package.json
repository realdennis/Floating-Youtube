{
  "name": "floating-youtube",
  "version": "2.5.3",
  "description": "Auto float video to corner when it's not in user's view.",
  "main": "index.js",
  "repository": "https://github.com/realdennis/Floating-Youtube.git",
  "author": "realdennis",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "rm -rf dist/ && mkdir dist &&  cp src/manifest.json dist/ && cp src/icon.png dist/ && parcel build src/index.js"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
